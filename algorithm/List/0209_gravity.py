T = int(input())
for tc in range(T):
    box_line = int(input())
    box = list(map(int, input().split()))

    # 박스들을 역순으로 꺼내봅니다.
    for bx in box[len(box), -1, -1]:
        # 제일 마지막 박스와 마지막에서 두번째 박스의 차이를 구해서 양수라면
        if bx - b